server {
    listen 8081;
    listen [::]:8081;
    server_name _;

    root /home/cino/Mini-CICD/runtime/deployments/react-vite/current;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
